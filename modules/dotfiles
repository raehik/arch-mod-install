# vim: set ft=sh
#
# Module:   dotfiles
# Priority: 99
# Desc.:    Install all my dotfiles (raehik/{dotfiles,adminfiles,grubfiles}).
#

for repo in $V_dotfile_list; do
    _log "git clone https://github.com/$V_github_user/$repo"
done
